// Stubs for filter transform functions
export function mapApiParamsToFilterFormWithPage(filters) {
  // Example: just return the filters as is
  return { filters };
}

export function mapFilterFormToApiParams(filters) {
  // Example: just return the filters as is
  return filters;
}
